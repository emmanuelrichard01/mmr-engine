# MMR Dashboard — Environment Variables
# Copy to .env.local and update values

# API base URL (default: local development)
NEXT_PUBLIC_API_URL=http://localhost:8000

# API key for authenticated endpoints (optional for read-only access)
NEXT_PUBLIC_API_KEY=
